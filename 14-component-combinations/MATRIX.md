<!-- GENERATED by scripts/gen_combination_matrix.py -- do not edit by hand. -->
---
topic: Component Combination Matrix
category: Combinations
difficulty: Intermediate
---

# Component Combination Matrix

Every pair of 18 components — all **153** of them — classified. Not a curated selection, so coverage is provable and the gaps are visible.

Literal *all permutations* would be 2<sup>18</sup> = 262,144 subsets, almost all meaningless (CDN + distributed lock). Pairs are the level at which exhaustiveness is both achievable and useful. [Meaningful triples](#meaningful-triples) are listed separately, because those are what real architectures are made of.

Every `CORE` and `REAL` pair names a real system and a public source. **A combination nobody ships is a thought experiment, not a pattern.**

| Class | Meaning | Count |
|---|---|---|
| ● **CORE** | Worth a full page of its own | 11 |
| ○ **REAL** | Genuinely used; documented below | 26 |
| · **SITU** | Occurs, but situational or often confused | 3 |
| ⚠ **ANTI** | A known mistake — documented so it is recognised | 3 |
| — **none** | No meaningful direct interaction | 110 |
| | **total** | **153** |

That the honest majority is *none* is the useful part. A matrix with no empty cells would mean the classification was not doing any work.

## The grid

Read either direction; the relation is symmetric.

| | `clie` | `cdn` | `lb` | `gate` | `serv` | `cach` | `db` | `repl` | `shar` | `queu` | `stre` | `work` | `blob` | `sear` | `rate` | `brea` | `lock` | `disc` |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **`clie`** | ▪ | ○ | ○ |  |  |  |  |  |  |  |  |  |  |  | ○ |  |  |  |
| **`cdn`** | ○ | ▪ | ● |  |  | · |  |  |  |  |  |  | ○ |  |  |  |  |  |
| **`lb`** | ○ | ● | ▪ |  | ○ | ● |  |  |  |  |  |  |  |  | ● |  |  | ○ |
| **`gate`** |  |  |  | ▪ | ○ |  | ⚠ |  |  |  |  |  |  |  | ○ | ○ |  |  |
| **`serv`** |  |  | ○ | ○ | ▪ | ○ | ○ |  |  | ○ |  |  |  |  |  | ● |  | ○ |
| **`cach`** |  | · | ● |  | ○ | ▪ | ● | ○ |  | ● | ○ |  |  |  |  |  | ⚠ |  |
| **`db`** |  |  |  | ⚠ | ○ | ● | ▪ | ● | ● | ● | ○ | ○ | ○ | ○ |  |  | · |  |
| **`repl`** |  |  |  |  |  | ○ | ● | ▪ | ● |  |  |  |  |  |  |  |  |  |
| **`shar`** |  |  |  |  |  |  | ● | ● | ▪ |  |  |  |  | ⚠ |  |  | ○ |  |
| **`queu`** |  |  |  |  | ○ | ● | ● |  |  | ▪ | · | ● |  |  |  | ○ |  |  |
| **`stre`** |  |  |  |  |  | ○ | ○ |  |  | · | ▪ | ○ |  | ○ |  |  |  |  |
| **`work`** |  |  |  |  |  |  | ○ |  |  | ● | ○ | ▪ | ○ |  |  |  | ○ |  |
| **`blob`** |  | ○ |  |  |  |  | ○ |  |  |  |  | ○ | ▪ |  |  |  |  |  |
| **`sear`** |  |  |  |  |  |  | ○ |  | ⚠ |  | ○ |  |  | ▪ |  |  |  |  |
| **`rate`** | ○ |  | ● | ○ |  |  |  |  |  |  |  |  |  |  | ▪ | ○ |  |  |
| **`brea`** |  |  |  | ○ | ● |  |  |  |  | ○ |  |  |  |  | ○ | ▪ |  |  |
| **`lock`** |  |  |  |  |  | ⚠ | · |  | ○ |  |  | ○ |  |  |  |  | ▪ |  |
| **`disc`** |  |  | ○ |  | ○ |  |  |  |  |  |  |  |  |  |  |  |  | ▪ |

| Key | | | | |
|---|---|---|---|---|
| ● CORE | ○ REAL | · SITU | ⚠ ANTI | ▪ self |

## Components

| id | Component | What it is |
|---|---|---|
| `clie` | Client | Browser, mobile app, or calling service |
| `cdn` | CDN / Edge | Geographically distributed cache |
| `lb` | Load Balancer | Spreads requests across servers |
| `gate` | API Gateway | Single entry point: auth, routing, quotas |
| `serv` | Service | Stateless application server |
| `cach` | Cache | Fast in-memory copy; safe to lose |
| `db` | Database | Durable primary store |
| `repl` | Read Replica | Read-only copy of the primary |
| `shar` | Shard | Horizontal partition of the data |
| `queu` | Queue | Point-to-point async work buffer |
| `stre` | Stream / Log | Ordered, replayable, multi-consumer log |
| `work` | Worker | Consumes async work |
| `blob` | Object Storage | Large immutable files |
| `sear` | Search Index | Inverted index for text and facets |
| `rate` | Rate Limiter | Caps consumption per client |
| `brea` | Circuit Breaker | Stops calling a failing dependency |
| `lock` | Distributed Lock | Mutual exclusion across processes |
| `disc` | Service Discovery | Finds healthy instances |

## ● Core combinations

These get their own page. Each one is a step in [the chain](../SYSTEM-DESIGN-THINKING.md#part-1--the-chain) — a problem created by the previous component.

| Combination | What emerges | Real system | Source |
|---|---|---|---|
| **CDN / Edge + Load Balancer** | The CDN absorbs cacheable traffic before it reaches you; the load balancer only sees the misses. Attacks the one term nothing else can touch: distance. | Netflix Open Connect | Netflix Open Connect appliance documentation |
| **Cache + Database** | The canonical latency fix, and the source of the canonical bug: invalidation. Adds staleness bounded by TTL, plus thundering herd when a hot key expires. | Facebook, Twitter, essentially every read-heavy product | Scaling Memcache at Facebook (NSDI '13) |
| **Cache + Queue** | The dangerous one. A cache miss storm and a queue backlog amplify each other: misses enqueue work, the backlog delays cache population, which produces more misses. | Common cascading-failure shape | Google SRE Book, ch. 22 (Addressing Cascading Failures) |
| **Database + Queue** | The dual-write problem: enqueue and commit are two systems and cannot share a transaction. Solved by the transactional outbox, not by ordering the two calls carefully. | Debezium / CDC-based pipelines | Kleppmann, Designing Data-Intensive Applications, ch. 11 |
| **Database + Read Replica** | Read scale, at the cost of replication lag -- which produces the most common consistency bug there is: a user writes, reads, and gets a 404 on their own record. | Standard Postgres/MySQL deployments | PostgreSQL streaming replication docs |
| **Database + Shard** | Write scale and unbounded storage, paid for with the loss of cross-shard joins and a shard key that is effectively permanent. | Discord message storage; Vitess at YouTube | Discord blog: How Discord Stores Trillions of Messages |
| **Load Balancer + Cache** | Where the cache sits decides whether it works. Per-server caches mean each server misses independently and hit rate falls as the fleet grows; a shared cache keeps one hit rate at any fleet size. | Facebook | Scaling Memcache at Facebook (NSDI '13) |
| **Load Balancer + Rate Limiter** | Limit at the edge or per-server? Per-server means N servers each allow the full limit. Shared state at the edge is correct but adds a hop to every request. | Stripe API rate limits | Stripe engineering blog, Scaling your API with rate limiters |
| **Queue + Worker** | Decouples arrival rate from processing rate, so a spike becomes a backlog instead of an outage. Buys at-least-once delivery, which forces idempotency. | Celery/Sidekiq shops; AWS SQS + Lambda | AWS SQS documentation |
| **Read Replica + Shard** | Shard for write scale, replicate each shard for read scale and availability. The standard large-database shape -- and now every shard has its own failover story. | Vitess; MongoDB sharded clusters | MongoDB sharded cluster documentation |
| **Service + Circuit Breaker** | Retries alone finish off a struggling dependency. The breaker is what makes retrying safe -- it converts 'slow' back into 'fast failure', which is the only mode you can route around. | Netflix Hystrix | Netflix TechBlog, Hystrix |

## ○ Real combinations

Genuinely shipped, documented here rather than given a page of their own.

| Combination | What emerges | Real system | Source |
|---|---|---|---|
| **API Gateway + Circuit Breaker** | One place to stop calls to a failing downstream, rather than every caller implementing it separately and inconsistently. | Netflix Zuul + Hystrix | Netflix TechBlog |
| **API Gateway + Rate Limiter** | Quota enforcement at the single entry point, where the identity of the caller is already known. | Kong, AWS API Gateway | AWS API Gateway throttling documentation |
| **API Gateway + Service** | One public entry point in front of many private services. Also becomes a single point of failure and a deployment bottleneck if it holds business logic. | Netflix Zuul | Netflix TechBlog, Zuul |
| **CDN / Edge + Object Storage** | Origin for static assets. The CDN serves; object storage stores. Cheapest possible delivery path for immutable content. | Most media delivery; S3 + CloudFront | AWS CloudFront + S3 documentation |
| **Cache + Read Replica** | Two staleness windows stacked: cache TTL plus replication lag. Users can observe data older than either bound alone. | Common read-heavy stack | Kleppmann, DDIA ch. 5 |
| **Cache + Stream / Log** | Cache invalidation driven by the change log instead of by application code. Turns a correctness problem into a plumbing problem. | Facebook's cache invalidation pipeline | Scaling Memcache at Facebook (NSDI '13) |
| **Client + CDN / Edge** | First hop for most user traffic. Everything downstream only sees what the edge could not answer. | Any consumer product at scale | Netflix Open Connect |
| **Client + Load Balancer** | First hop when there is no edge tier. DNS decides which load balancer, which makes DNS part of your availability calculation. | Standard web architecture | AWS ELB documentation |
| **Client + Rate Limiter** | A limiter that says no without saying when guarantees an immediate retry. 429 plus Retry-After is the contract. | Stripe, GitHub APIs | GitHub REST API rate limit documentation |
| **Database + Object Storage** | Metadata in the database, bytes in object storage. Storing large blobs in the database inflates backups and destroys buffer-cache efficiency. | Standard media architecture | AWS S3 best practices |
| **Database + Search Index** | The database owns the truth; the index owns the query. They are always slightly out of sync, and the sync mechanism is where the bugs are. | Most e-commerce catalogues | Elasticsearch documentation |
| **Database + Stream / Log** | Change data capture: the database's replication log becomes the event stream, which removes the dual-write problem entirely. | Debezium; Netflix DBLog | Netflix TechBlog, DBLog |
| **Database + Worker** | Async writes. Workers can overwhelm a primary that the request path was politely rate-limited against -- the queue removed the natural backpressure. | Common failure in batch pipelines | Kleppmann, DDIA ch. 11 |
| **Load Balancer + Service** | Requires the service to be stateless. Sessions in local memory break load balancing, deploys and failover simultaneously. | Every horizontally scaled tier | Twelve-Factor App, factor VI |
| **Load Balancer + Service Discovery** | The load balancer needs to know which instances exist and which are healthy. In autoscaled fleets that list changes constantly. | Consul, Kubernetes Services | Kubernetes Service documentation |
| **Queue + Circuit Breaker** | When the breaker opens, work can be parked on a queue instead of dropped -- so the outage degrades to a delay. | Common resilience shape | Google SRE Book, ch. 22 |
| **Rate Limiter + Circuit Breaker** | Rate limiting protects you from callers; the circuit breaker protects you from dependencies. Inbound and outbound halves of the same idea. | Netflix Hystrix + Zuul | Netflix TechBlog |
| **Service + Cache** | Read-through or cache-aside. Cache-aside puts invalidation in application code, which is where invalidation bugs live. | Ubiquitous | Kleppmann, DDIA ch. 1 |
| **Service + Database** | The base case. Connection pool size, not database capacity, is usually the real throughput ceiling. | Every system | Little's Law |
| **Service + Queue** | Producer side. Moving work off the request path is the single largest perceived latency win available. | Ubiquitous | AWS SQS documentation |
| **Service + Service Discovery** | Services register; callers resolve. In Kubernetes this is DNS plus endpoint objects rather than a separate system. | Kubernetes, Consul | Kubernetes Service documentation |
| **Shard + Distributed Lock** | Resharding needs mutual exclusion or two nodes will move the same key. One of the few places a distributed lock is genuinely warranted. | Vitess resharding | Vitess resharding documentation |
| **Stream / Log + Search Index** | Index updates fed from the change log rather than by dual-writing from the application. | Common CDC-to-Elasticsearch pipeline | Debezium documentation |
| **Stream / Log + Worker** | Unlike a queue, the log is replayable and multi-consumer -- so a bug can be fixed by reprocessing from an offset rather than by recovering lost messages. | LinkedIn (Kafka's origin) | Kreps, The Log (LinkedIn engineering, 2013) |
| **Worker + Distributed Lock** | Ensures only one worker handles a given key. Often replaceable by partitioning the key space, which needs no lock at all. | Kafka consumer group partition assignment | Kafka documentation |
| **Worker + Object Storage** | Large payloads go to object storage; the queue carries only a pointer. Avoids message size limits and keeps the queue fast. | Claim-check pattern; AWS SQS extended client | Azure Architecture Center, Claim-Check pattern |

## · Situational

Occurs, but niche — or commonly confused with something else.

| Combination | What emerges | Real system | Source |
|---|---|---|---|
| **CDN / Edge + Cache** | Two cache layers with independent TTLs. Debugging staleness means reasoning about both at once, and the edge TTL usually dominates. | Multi-layer caching setups | Fastly / Cloudflare caching docs |
| **Database + Distributed Lock** | Often unnecessary: a database transaction or a unique constraint usually does the job with fewer moving parts and no lease-expiry hazard. | Frequently over-applied | Kleppmann, How to do distributed locking (2016) |
| **Queue + Stream / Log** | Frequently confused. A queue deletes on ack and is point-to-point; a log retains and supports many independent consumers. Picking wrongly is a common and expensive mistake. | Kafka vs RabbitMQ decisions | Kreps, The Log |

## ⚠ Anti-patterns

Documented so they are recognised in a design review, not so they are copied.

| Combination | What emerges | Real system | Source |
|---|---|---|---|
| **API Gateway + Database** | A gateway reaching a database directly means business logic has leaked into the edge tier, which then cannot be deployed independently. | Distributed-monolith smell | Richardson, Microservices Patterns |
| **Cache + Distributed Lock** | Locking to prevent a thundering herd usually serialises every reader behind one lock. Request coalescing or a probabilistic early refresh solves it without the contention. | Common over-correction | Vattani et al., Optimal Probabilistic Cache Stampede Prevention (VLDB 2015) |
| **Shard + Search Index** | Fanning a search across every shard makes latency the slowest shard's latency, every time. Use a dedicated index instead. | Common scatter-gather trap | Dean & Barroso, The Tail at Scale (CACM 2013) |

## Meaningful triples

Pairs are where the interactions are; triples are where the architectures are.

| Combination | What emerges | Real system | Source |
|---|---|---|---|
| ● **Load Balancer + Cache + Database** | The default read-heavy web stack. Each element solves the problem the previous one created. | Almost every consumer web product | Scaling Memcache at Facebook |
| ● **CDN / Edge + Load Balancer + Cache** | Three cache layers at three distances. Debugging staleness means reasoning about all three TTLs together. | Netflix, Cloudflare-fronted products | Netflix Open Connect |
| ● **Queue + Worker + Database** | The async write pipeline. At-least-once delivery makes idempotency mandatory, not optional. | Stripe webhook processing | Stripe idempotency documentation |
| ● **Cache + Queue + Database** | Write-behind caching. Fastest writes available, and the only shape here that can lose acknowledged data. | High-write telemetry systems | Kleppmann, DDIA ch. 11 |
| ○ **Shard + Read Replica + Load Balancer** | Full database scale-out: partitioned for writes, replicated for reads, routed by a shard-aware layer. | Vitess at YouTube | Vitess documentation |
| ● **Circuit Breaker + Queue + Worker** | The resilience triad. Retry without a breaker is a retry storm; a breaker without a queue drops work that could have waited. | Netflix Hystrix + SQS-style buffers | Google SRE Book ch. 22 |
| ○ **Stream / Log + Worker + Search Index** | CDC-driven index maintenance. Removes the dual write between database and index. | Debezium to Elasticsearch | Debezium documentation |
| ○ **API Gateway + Rate Limiter + Circuit Breaker** | The edge policy tier: who may call, how often, and what happens when downstream is sick. | Netflix Zuul | Netflix TechBlog |

## Pairs with no meaningful interaction

The remaining **110** pairs. Listed rather than omitted, so that 'not covered' is distinguishable from 'not applicable'.

- Client + API Gateway · Client + Service · Client + Cache
- Client + Database · Client + Read Replica · Client + Shard
- Client + Queue · Client + Stream / Log · Client + Worker
- Client + Object Storage · Client + Search Index · Client + Circuit Breaker
- Client + Distributed Lock · Client + Service Discovery · CDN / Edge + API Gateway
- CDN / Edge + Service · CDN / Edge + Database · CDN / Edge + Read Replica
- CDN / Edge + Shard · CDN / Edge + Queue · CDN / Edge + Stream / Log
- CDN / Edge + Worker · CDN / Edge + Search Index · CDN / Edge + Rate Limiter
- CDN / Edge + Circuit Breaker · CDN / Edge + Distributed Lock · CDN / Edge + Service Discovery
- Load Balancer + API Gateway · Load Balancer + Database · Load Balancer + Read Replica
- Load Balancer + Shard · Load Balancer + Queue · Load Balancer + Stream / Log
- Load Balancer + Worker · Load Balancer + Object Storage · Load Balancer + Search Index
- Load Balancer + Circuit Breaker · Load Balancer + Distributed Lock · API Gateway + Cache
- API Gateway + Read Replica · API Gateway + Shard · API Gateway + Queue
- API Gateway + Stream / Log · API Gateway + Worker · API Gateway + Object Storage
- API Gateway + Search Index · API Gateway + Distributed Lock · API Gateway + Service Discovery
- Service + Read Replica · Service + Shard · Service + Stream / Log
- Service + Worker · Service + Object Storage · Service + Search Index
- Service + Rate Limiter · Service + Distributed Lock · Cache + Shard
- Cache + Worker · Cache + Object Storage · Cache + Search Index
- Cache + Rate Limiter · Cache + Circuit Breaker · Cache + Service Discovery
- Database + Rate Limiter · Database + Circuit Breaker · Database + Service Discovery
- Read Replica + Queue · Read Replica + Stream / Log · Read Replica + Worker
- Read Replica + Object Storage · Read Replica + Search Index · Read Replica + Rate Limiter
- Read Replica + Circuit Breaker · Read Replica + Distributed Lock · Read Replica + Service Discovery
- Shard + Queue · Shard + Stream / Log · Shard + Worker
- Shard + Object Storage · Shard + Rate Limiter · Shard + Circuit Breaker
- Shard + Service Discovery · Queue + Object Storage · Queue + Search Index
- Queue + Rate Limiter · Queue + Distributed Lock · Queue + Service Discovery
- Stream / Log + Object Storage · Stream / Log + Rate Limiter · Stream / Log + Circuit Breaker
- Stream / Log + Distributed Lock · Stream / Log + Service Discovery · Worker + Search Index
- Worker + Rate Limiter · Worker + Circuit Breaker · Worker + Service Discovery
- Object Storage + Search Index · Object Storage + Rate Limiter · Object Storage + Circuit Breaker
- Object Storage + Distributed Lock · Object Storage + Service Discovery · Search Index + Rate Limiter
- Search Index + Circuit Breaker · Search Index + Distributed Lock · Search Index + Service Discovery
- Rate Limiter + Distributed Lock · Rate Limiter + Service Discovery · Circuit Breaker + Distributed Lock
- Circuit Breaker + Service Discovery · Distributed Lock + Service Discovery

## Related

- [System Design Thinking](../SYSTEM-DESIGN-THINKING.md) — the chain these sit on
- [Trade-off Framework](../TRADEOFF-FRAMEWORK.md)
- [Glossary](../GLOSSARY.md)
